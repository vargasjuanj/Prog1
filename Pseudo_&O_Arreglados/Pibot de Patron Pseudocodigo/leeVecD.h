#ifndef LEEVECD_H
#define LEEVECD_H
#include <program1.h>
plantilla(Tipo)
procedimiento leeMatD(entero,entero, matrizDin2D(Tipo) porRef );
plantilla(Tipo)
procedimiento leeVecD(entero, vectorDin(Tipo) porRef );
plantilla(Tipo)
procedimiento leeVecD(vectorDin(Tipo) porRef );
plantilla(Tipo)
procedimiento generaVecAlAzar(vectorDin(Tipo) porRef );
plantilla(Tipo)
procedimiento generaVecAlAzarCre(vectorDin(Tipo) porRef );
#endif

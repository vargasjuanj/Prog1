// Archivo de traducción de seudocódigo a C++
#include <program1.h>
/**
*    Enunciado: Procedimiento que intercambia los valores de dos variables pasadas por referencia.
*/
plantilla(Tipo)
procedimiento intercambiar(Tipo porRef x, Tipo porRef y) {
Tipo z = x;
x = y;
y = z;
}
//implementa procedimiento intercambiar(doble porRef,doble porRef);
implementa procedimiento intercambiar(real porRef,real porRef);
//implementa procedimiento intercambiar(entero porRef,entero porRef);
